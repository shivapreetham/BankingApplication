# Banking System - Architecture & Flow Diagrams

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                          CLIENT LAYER                            │
│  (Web Browser, Mobile App, Postman, cURL, etc.)                 │
└────────────────────────────┬────────────────────────────────────┘
                             │
                    HTTP/HTTPS Requests
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    SPRING BOOT APPLICATION                       │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  REST CONTROLLERS (Request Handlers)                     │    │
│  │  ├── AuthController        (/auth/*)                    │    │
│  │  ├── AccountController     (/accounts/*)                │    │
│  │  └── TransactionController (/transactions/*)            │    │
│  └──────────────────────┬──────────────────────────────────┘    │
│                        │                                         │
│                        ▼                                         │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  SECURITY LAYER                                         │    │
│  │  ├── JwtAuthenticationFilter (Extract Token)            │    │
│  │  ├── SecurityConfig         (Configure Security)        │    │
│  │  └── JwtTokenProvider       (Generate/Validate Token)   │    │
│  └──────────────────────┬──────────────────────────────────┘    │
│                        │                                         │
│                        ▼                                         │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  BUSINESS LOGIC (Service Layer)                         │    │
│  │  ├── UserService       (Auth, Profile)                  │    │
│  │  ├── AccountService    (Account Management)             │    │
│  │  └── TransactionService (Deposit, Withdraw, Transfer)   │    │
│  └──────────────────────┬──────────────────────────────────┘    │
│                        │                                         │
│                        ▼                                         │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  REPOSITORY LAYER (Data Access)                         │    │
│  │  ├── UserRepository       (User CRUD)                   │    │
│  │  ├── AccountRepository    (Account CRUD)                │    │
│  │  └── TransactionRepository (Transaction Queries)        │    │
│  └──────────────────────┬──────────────────────────────────┘    │
│                        │                                         │
│                        ▼                                         │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  EXCEPTION HANDLING                                     │    │
│  │  └── GlobalExceptionHandler (Format Error Responses)    │    │
│  └──────────────────────┬──────────────────────────────────┘    │
└─────────────────────────┼────────────────────────────────────────┘
                         │
               JDBC/JPA Queries
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                     HIBERNATE ORM                                │
│     (Object-Relational Mapping)                                 │
└────────────────────────────┬────────────────────────────────────┘
                             │
                        SQL Queries
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      MySQL DATABASE                              │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐    │
│  │   Users Table  │  │ Accounts Table │  │Transactions TB │    │
│  │                │  │                │  │                │    │
│  │ • user_id      │  │ • account_id   │  │ • trans_id     │    │
│  │ • username     │  │ • user_id (FK) │  │ • account_id(FK)│   │
│  │ • password     │  │ • account_num  │  │ • type         │    │
│  │ • email        │  │ • balance      │  │ • amount       │    │
│  │ • phone        │  │ • status       │  │ • timestamp    │    │
│  │ • address      │  │ • created_date │  │ • description  │    │
│  └────────────────┘  └────────────────┘  └────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Authentication & Authorization Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. USER REGISTRATION / LOGIN                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  POST /auth/register                                            │
│  {                                                              │
│    "username": "john",                                         │
│    "password": "pass123",                                      │
│    "email": "john@example.com"                                 │
│  }                                                              │
│                                                                   │
│           │                                                      │
│           ▼                                                      │
│  ┌──────────────────────────────────────────┐                  │
│  │ UserService.registerUser()               │                  │
│  │ ├─ Check username uniqueness             │                  │
│  │ ├─ Check email uniqueness                │                  │
│  │ ├─ Encode password with BCrypt           │                  │
│  │ └─ Save User entity to database          │                  │
│  └──────────────────────┬───────────────────┘                  │
│                         │                                       │
│                         ▼                                       │
│  ┌──────────────────────────────────────────┐                  │
│  │ JwtTokenProvider.generateToken()         │                  │
│  │ ├─ Create JWT with userId as subject     │                  │
│  │ ├─ Add username as claim                 │                  │
│  │ ├─ Set 24-hour expiration                │                  │
│  │ ├─ Sign with HS512 algorithm             │                  │
│  │ └─ Return signed token                   │                  │
│  └──────────────────────┬───────────────────┘                  │
│                         │                                       │
│                         ▼                                       │
│  Return AuthResponse:                                           │
│  {                                                              │
│    "token": "eyJhbGciOiJIUzUxMiJ9...",                        │
│    "userId": 1,                                                │
│    "username": "john",                                         │
│    "email": "john@example.com"                                │
│  }                                                              │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ 2. SUBSEQUENT API REQUESTS WITH JWT TOKEN                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  Client makes request with token:                              │
│  GET /accounts                                                 │
│  Authorization: Bearer eyJhbGciOiJIUzUxMiJ9...                │
│                                                                   │
│           │                                                      │
│           ▼                                                      │
│  ┌──────────────────────────────────────────┐                  │
│  │ JwtAuthenticationFilter.doFilterInternal │                  │
│  │ ├─ Extract Authorization header          │                  │
│  │ ├─ Verify "Bearer " prefix               │                  │
│  │ └─ Extract token string                  │                  │
│  └──────────────────────┬───────────────────┘                  │
│                         │                                       │
│                         ▼                                       │
│  ┌──────────────────────────────────────────┐                  │
│  │ JwtTokenProvider.validateToken()         │                  │
│  │ ├─ Parse token                           │                  │
│  │ ├─ Verify signature                      │                  │
│  │ ├─ Check expiration                      │                  │
│  │ └─ Return true/false                     │                  │
│  └──────────────────────┬───────────────────┘                  │
│                         │                                       │
│                      Valid? ──────────┐                        │
│                     /        \         │                        │
│                  Yes          No       │                        │
│                   │             │      │                        │
│                   ▼             ▼      │                        │
│  ┌──────────────────┐  ┌──────────────────┐                   │
│  │ Extract userId & │  │ Return 401       │                   │
│  │ username         │  │ Unauthorized     │                   │
│  │                  │  │                  │                   │
│  │ Create Authentication                  │                   │
│  │ object           │  └──────────────────┘                   │
│  │                  │                                          │
│  │ Set in           │                                          │
│  │ SecurityContext  │                                          │
│  └────────┬─────────┘                                          │
│           │                                                     │
│           ▼                                                     │
│  Proceed to controller with authenticated context              │
│  Controller can access username via:                           │
│  Authentication auth = SecurityContextHolder.getContext()     │
│                       .getAuthentication();                   │
│  String username = auth.getName();                            │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 💰 Transaction Flow

```
┌────────────────────────────────────────────────────────────────┐
│ DEPOSIT TRANSACTION                                             │
├────────────────────────────────────────────────────────────────┤
│                                                                  │
│  POST /transactions/deposit                                    │
│  {                                                             │
│    "accountNumber": "ACC123...",                              │
│    "amount": 1000.00                                          │
│  }                                                             │
│                                         │                     │
│                                         ▼                     │
│                         ┌───────────────────────────┐          │
│                         │ Validate amount > 0       │          │
│                         │ Find account by number    │          │
│                         │ Check ownership           │          │
│                         └────────┬──────────────────┘          │
│                                  │                             │
│                                  ▼                             │
│                    ┌──────────────────────────┐                │
│                    │ Update Account Balance   │                │
│                    │ newBalance = old + 1000  │                │
│                    │ Save to database         │                │
│                    └────────┬─────────────────┘                │
│                             │                                  │
│                             ▼                                  │
│                   ┌─────────────────────────┐                 │
│                   │ Create Transaction      │                 │
│                   │ Type: DEPOSIT           │                 │
│                   │ Amount: 1000            │                 │
│                   │ BalanceAfter: new      │                 │
│                   │ Save to database        │                 │
│                   └────────┬────────────────┘                 │
│                            │                                  │
│                            ▼                                  │
│                   Return TransactionResponse                 │
│                                                                  │
│  ┌─────────────────────────────────────────────────┐           │
│  │ WITHDRAWAL TRANSACTION (with balance check)      │           │
│  │                                                  │           │
│  │ Same as deposit but:                            │           │
│  │ ├─ Check balance >= amount                      │           │
│  │ ├─ newBalance = old - amount                    │           │
│  │ └─ Type: WITHDRAWAL                             │           │
│  └─────────────────────────────────────────────────┘           │
│                                                                  │
│  ┌─────────────────────────────────────────────────┐           │
│  │ TRANSFER TRANSACTION (between 2 accounts)        │           │
│  │                                                  │           │
│  │ ├─ Validate both accounts exist                 │           │
│  │ ├─ Check source account has sufficient balance  │           │
│  │ ├─ @Transactional ensures atomic operation      │           │
│  │ ├─ Deduct from source account                   │           │
│  │ ├─ Add to destination account                   │           │
│  │ ├─ Record 2 transactions (FROM and TO)          │           │
│  │ └─ Commit all changes or rollback all           │           │
│  └─────────────────────────────────────────────────┘           │
│                                                                  │
└────────────────────────────────────────────────────────────────┘
```

---

## 📋 Database Relationships

```
Users (1) ──── (Many) Accounts
   │                   │
   │                   │ 1 Account has Many Transactions
   │                   │
   │                   └── (Many) Transactions
   │
   └── Every User has:
       • Multiple Accounts (SAVINGS, CHECKING, etc.)
       • Unique username & email
       • BCrypt encrypted password


Accounts (1) ──── (Many) Transactions
   │
   └── Every Account has:
       • Unique account number (ACC+random)
       • Current balance
       • Status (ACTIVE/CLOSED)
       • Audit timestamps


Transactions (Many) ──── (1) Account
   │
   └── Every Transaction:
       • Linked to specific account
       • Type (DEPOSIT/WITHDRAWAL/TRANSFER)
       • Amount and new balance
       • Timestamp for audit trail
```

---

## 🔄 Request-Response Cycle

```
┌────────────────────────────────────────────────────────────┐
│ CLIENT                                                      │
│ (Browser, Postman, Mobile App)                            │
└────────────────┬─────────────────────────────────────────┘
                 │
                 │ 1. HTTP Request
                 │ (Method, URL, Headers, Body)
                 ▼
┌────────────────────────────────────────────────────────────┐
│ REQUEST PROCESSING                                         │
│ ├─ DispatcherServlet routes to controller                 │
│ ├─ JwtAuthenticationFilter validates token                │
│ ├─ Security check: is endpoint public/protected?          │
│ └─ Controller receives request                            │
└────────────────┬─────────────────────────────────────────┘
                 │
                 │ 2. Business Logic
                 ▼
┌────────────────────────────────────────────────────────────┐
│ BUSINESS LOGIC                                             │
│ ├─ Service method executes                                │
│ ├─ Repository queries database                            │
│ ├─ Validation & error checking                            │
│ ├─ If error: GlobalExceptionHandler formats error        │
│ └─ If success: DTO converts entity to response format    │
└────────────────┬─────────────────────────────────────────┘
                 │
                 │ 3. HTTP Response
                 │ (Status Code, Headers, Body JSON)
                 ▼
┌────────────────────────────────────────────────────────────┐
│ RESPONSE CODES                                             │
│ ├─ 200 OK: Successful GET/PUT                            │
│ ├─ 201 CREATED: Successful POST                          │
│ ├─ 204 NO CONTENT: Successful DELETE                     │
│ ├─ 400 BAD REQUEST: Validation error                     │
│ ├─ 401 UNAUTHORIZED: Missing/invalid token               │
│ ├─ 403 FORBIDDEN: Resource not owned by user             │
│ ├─ 404 NOT FOUND: Resource doesn't exist                │
│ └─ 500 INTERNAL SERVER ERROR: Unexpected error           │
└────────────────┬─────────────────────────────────────────┘
                 │
                 │ 4. Response Body (JSON)
                 ▼
┌────────────────────────────────────────────────────────────┐
│ CLIENT                                                      │
│ Parse JSON, update UI, display data/error                │
└────────────────────────────────────────────────────────────┘
```

---

## 📊 Entity Relationship Diagram

```
┌─────────────────────────┐
│        User             │
├─────────────────────────┤
│ • user_id (PK)          │
│ • username (UNIQUE)     │
│ • password (encrypted)  │
│ • email (UNIQUE)        │
│ • phone                 │
│ • address               │
│ • created_date          │
│ • last_modified_date    │
└────────┬────────────────┘
         │ (1:Many)
         │ One user has many accounts
         │
         ▼
┌─────────────────────────┐
│      Account            │
├─────────────────────────┤
│ • account_id (PK)       │
│ • user_id (FK)          │
│ • account_number (UNIQ) │
│ • account_type          │
│ • balance               │
│ • status                │
│ • created_date          │
│ • last_modified_date    │
└────────┬────────────────┘
         │ (1:Many)
         │ One account has many transactions
         │
         ▼
┌─────────────────────────┐
│    Transaction          │
├─────────────────────────┤
│ • trans_id (PK)         │
│ • account_id (FK)       │
│ • trans_type            │
│ • amount                │
│ • balance_after         │
│ • description           │
│ • timestamp             │
└─────────────────────────┘
```

---

## 🛡️ Security Architecture

```
┌─────────────────────────────────────────────────────────────┐
│ SPRING SECURITY CONFIGURATION                               │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  SecurityConfig (@Configuration)                            │
│  ├─ FilterChain                                             │
│  │  ├─ CSRF disabled (stateless REST API)                   │
│  │  ├─ SessionCreationPolicy = STATELESS                    │
│  │  ├─ CORS enabled                                         │
│  │  ├─ Authorization rules:                                 │
│  │  │  ├─ /auth/** = permitAll (public)                    │
│  │  │  └─ /** = authenticated (protected)                  │
│  │  └─ Add JwtAuthenticationFilter                          │
│  │                                                           │
│  ├─ PasswordEncoder                                          │
│  │  └─ BCryptPasswordEncoder (strength: 10)                 │
│  │     ├─ randomSalt + iteration                            │
│  │     ├─ secure password hashing                           │
│  │     └─ one-way encryption                                │
│  │                                                           │
│  └─ CORS Configuration                                       │
│     ├─ Allowed Origins: localhost:3000, localhost:8080      │
│     ├─ Allowed Methods: GET, POST, PUT, DELETE, OPTIONS     │
│     ├─ Allowed Headers: *                                   │
│     └─ Max Age: 3600 seconds                                │
│                                                               │
└────────────────────────┬──────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│ JWT TOKEN SECURITY                                          │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  Token Structure:                                            │
│  ┌──────────────┬──────────────┬──────────────┐             │
│  │   Header     │    Payload   │   Signature  │             │
│  ├──────────────┼──────────────┼──────────────┤             │
│  │ {            │ {            │              │             │
│  │ "alg":       │ "sub": "1",  │ HMACSHA512(  │             │
│  │ "HS512"      │ "name": "j", │ secret,      │             │
│  │ }            │ "exp": 1234  │ header.      │             │
│  │              │ }            │ payload)     │             │
│  └──────────────┴──────────────┴──────────────┘             │
│                                                               │
│  Validation:                                                 │
│  ├─ Signature verification (HS512)                           │
│  ├─ Expiration check (24 hours)                             │
│  ├─ Subject extraction (userId)                             │
│  └─ Claim extraction (username)                             │
│                                                               │
└────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ PASSWORD SECURITY                                           │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  Original Password: "pass123"                               │
│  ├─ Generate random salt                                    │
│  ├─ Apply BCrypt algorithm with salt                        │
│  ├─ Iterate 10 times (default strength)                     │
│  └─ Result: $2a$10$abcd1234...xyz (one-way hash)          │
│                                                               │
│  Authentication Check:                                       │
│  ├─ User submits: "pass123"                                │
│  ├─ Extract salt from stored hash                           │
│  ├─ Apply BCrypt to submitted password with same salt       │
│  ├─ Compare results                                         │
│  └─ True/False result                                       │
│                                                               │
│  Never stores plain passwords ✓                             │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 💾 Database Operations Flow

```
SERVICE LAYER (Business Logic)
        │
        ▼
REPOSITORY INTERFACE
    (JpaRepository<T, ID>)
        │
        ├── save()          ──► INSERT/UPDATE
        ├── findById()      ──► SELECT WHERE id = ?
        ├── findAll()       ──► SELECT *
        ├── delete()        ──► DELETE WHERE id = ?
        └── Custom methods  ──► Custom queries
        │
        ▼
HIBERNATE ORM
(Object-Relational Mapping)
    ├─ Convert Java objects to SQL
    ├─ Map results to objects
    ├─ Handle relationships
    └─ Transaction management
        │
        ▼
JDBC DRIVER
(MySQL Connector)
        │
        ▼
MySQL DATABASE
    ├─ Execute SQL queries
    ├─ Maintain ACID properties
    └─ Return result sets
```

---

## 📈 Deployment Architecture

```
┌──────────────────────────────────────────────────────────┐
│                    PRODUCTION ENVIRONMENT                │
├──────────────────────────────────────────────────────────┤
│                                                            │
│  ┌────────────────────────────────────────────────────┐  │
│  │ Load Balancer (Optional)                           │  │
│  │ ├─ Distribute traffic                              │  │
│  │ ├─ HTTPS termination                               │  │
│  │ └─ SSL certificates                                │  │
│  └────────────────┬─────────────────────────────────┘  │
│                   │                                      │
│        ┌──────────┴───────────┬─────────────┐           │
│        ▼                      ▼             ▼           │
│  ┌───────────┐         ┌───────────┐  ┌───────────┐    │
│  │ Instance1 │         │ Instance2 │  │ Instance3 │    │
│  │ Spring    │         │ Spring    │  │ Spring    │    │
│  │ Boot App  │         │ Boot App  │  │ Boot App  │    │
│  └──────┬────┘         └──────┬────┘  └──────┬────┘    │
│         │                     │               │         │
│         └─────────────────────┴───────────────┘         │
│                               │                         │
│         ┌─────────────────────┘                         │
│         │  (Database Connection String)                 │
│         ▼                                               │
│  ┌──────────────────────────────┐                       │
│  │  MySQL Database              │                       │
│  │  ├─ Replicated for HA        │                       │
│  │  ├─ Backup strategy          │                       │
│  │  └─ Connection pool          │                       │
│  └──────────────────────────────┘                       │
│                                                          │
│  ┌──────────────────────────────┐                       │
│  │  Monitoring & Logging        │                       │
│  │  ├─ Application logs         │                       │
│  │  ├─ Database logs            │                       │
│  │  ├─ Performance metrics      │                       │
│  │  └─ Alert system             │                       │
│  └──────────────────────────────┘                       │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

---

**These diagrams provide a comprehensive visual understanding of the Banking System architecture, security, data flow, and deployment topology.**
